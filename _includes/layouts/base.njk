<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <link rel="icon" href="{{ '/favicon.ico' | url }}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    <script src="/scripts/main.js" defer></script>
    <title>{{ title | default("WELCOME TO THE FOURTH POLE") }}</title>
  </head>
  <body>
    <div id="pageContainer">
      <header>
        <a href="{{ '/' | url }}">
          
          <picture>
            <source media="(width >= 1024px)" srcset="{{ '/imgs/secreto_logo_horiz_lg.webp' | url }}">
            <source media="(width >= 750px)" srcset="{{ '/imgs/secreto_logo_horiz_med.webp' | url }}">
            <source media="(width < 550px)" srcset="{{ '/imgs/secreto_logo_horiz_sm.webp' | url }}">
            <img src="{{ '/imgs/secreto_logo_horiz_lg.webp' | url }}" alt="shitty secret santa logo" id="logo" />
          </picture>
        </a>
        <menu>
          <ul>
            <li id="homeLink"><a class="{{ 'selected' if page.url === '/' }}" href="{{ '/' | url }}"><strong>HOME</strong></a></li>
            <li><a class="{{ 'selected' if page.url === '/rules/' }}" href="{{ '/rules' | url }}"><strong>RULES</strong></a></li>
            <li><a class="{{ 'selected' if page.url === '/gifts/' }}" href="{{ '/gifts' | url }}"><strong>GIFTS</strong></a></li>
            <li><a class="{{ 'selected' if page.url === '/players/' }}" href="{{ '/players' | url }}"><strong>PLAYERS</strong></a></li>
          </ul>
        </menu>
      </header>

      <div class="mainRow">
        <div class="bannerContainer"><img src="" alt=""  id="leftBanner"/></div>
        <main>
        {{ content | safe }}
        </main>
        <div class="bannerContainer"><img src="" alt=""  id="rightBanner"/></div>
      </div>

      <footer>
        <div id="footerToggles">
        <div>
          <span>DARK MODE</span>
          <label class="toggle">
            <input id="darkMode" type="checkbox">
            <span class="slider"></span>
          </label>
        </div>
        <div>
          <span>AD MODE</span>
          <label class="toggle">
            <input id="adMode" type="checkbox" checked>
            <span class="slider"></span>
          </label>
        </div>
        </div>
        <span id="footerText">Shitty Secret Santa. Est 2010.</span>
      </footer>
    </div>
  </body>
</html>